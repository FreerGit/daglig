open! Core
open! Lib
(*
   let () =
  Eio_main.run
  @@ fun stdenv ->
  Eio.Switch.run
  @@ fun sw ->
  Database.Migration.run_migrations
    ~sw
    ~stdenv:(stdenv :> Caqti_eio.stdenv)
    ~uri:Uri.empty
;; *)
